# Provisioning Grafana — datasource Prometheus (auto-chargée au démarrage).
# Grafana LOCAL, aucune dépendance GCP.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
